en:
  activerecord:
    errors:
      models:
        user:
          attributes:
            email:
              blank: Empty Email
            first_name:
              blank: Empty name
            password:
              blank: Empty password
            birthdate:
              blank: Empty birthdate
  errors:
    token:
      invalid: Invalid token
      expired: Expired token
    user:
      not_found: User with ID=%{id} not found
    product:
      not_found: Product with ID=%{product_id} in category ID=%{product_category_id} not found
    product_category:
      not_found: Product category with ID=%{product_category_id} not found
    api:
      accept: Invalid data type in Accept header. Supported only 'application/json'
      content_type: Invalid data type in Content-type header. Supported only 'application/json'
